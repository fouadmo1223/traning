// let range = window.prompt("Enter a range (e.g., 1-10)");
// console.log(range);

// let arrOfRange = range.split("-");
// let min, max;

// // Convert the string values to numbers for comparison
// if (parseInt(arrOfRange[0]) > parseInt(arrOfRange[1])) {
//   [max, min] = arrOfRange.map(Number);
// } else {
//   [min, max] = arrOfRange.map(Number);
// }

// console.log("Max:", max);
// console.log("Min:", min);

// // Loop from min to max and write the numbers to the document
// for (let i = min; i <= max; i++) {
//   document.write(`<h3>${i}</h3>`);
// }
////////////////////////////////////////////////////////////////////////////////

// let closeElem = document.querySelector(".popup .close");

// setTimeout(function () {
//   document.querySelector(".popup").style.display = "flex"; // Set display to "flex"
// }, 3000);

// closeElem.addEventListener("click", function (e) {
//   e.target.parentElement.remove(); // Remove the parent element on click
// });

///////////////////////////////////////
// let i = 10;
// setInterval(function () {
//   document.querySelector(`.counter`).innerHTML = i;
//   if (i != 0) {
//     i--;
//   } else {
//     window.location.href = "https://elzero.org/category/assignments/javascript-bootcamp-assignments/";
//     // clearInterval(1);
//   }
// }, 1000);
// /////////////////////////////////
